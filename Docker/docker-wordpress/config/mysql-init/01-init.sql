-- İsteğe bağlı: Veritabanı oluşturma scripti
-- Bu dosya, MySQL container'ı ilk başlatıldığında çalıştırılacaktır
-- Gerektiğinde özel SQL sorgularınızı buraya ekleyebilirsiniz

-- Örnek: Varsayılan bir kullanıcı oluşturma
-- CREATE USER IF NOT EXISTS 'wordpress'@'%' IDENTIFIED BY 'wordpress';
-- GRANT ALL PRIVILEGES ON wordpress.* TO 'wordpress'@'%';
-- FLUSH PRIVILEGES;
